# 安全策略（Security Policy）

## 报告漏洞

本项目是本地单机的个人/教育项目。公开后如发现安全问题，请通过 GitHub Issues 提交报告（敏感信息请先脱敏），并说明：

- 影响版本或提交号
- 复现步骤
- 影响范围（是否涉及密钥、答卷数据泄露）

## 安全设计要点

- **密钥加密存储**：模型 API Key 使用 AES-256-GCM 加密后存于本机 `.data/`，不返回浏览器、不写入 Git
- **接口鉴权**：所有 `/api` 请求校验 `HENGZHUN_API_TOKEN`（timingSafeEqual 比较）
- **出站白名单**：外呼模型 URL 必须通过 `outboundUrlPolicy` 白名单校验
- **日志脱敏**：不记录 API Key 与图片 Base64；图片只记录类型、字节数与 SHA-256 摘要
- **不提交的目录**：`.data/`、`.env*`、`node_modules/`、`dist*`、`release*`、`tmp/` 均不进入 Git

## 本地单机的使用边界

本项目为**本地单机工具**，请勿将 API 服务直接暴露到公网（服务默认只监听 `127.0.0.1`）。模型配置、学生答卷与评分历史属于敏感数据，请妥善保管 `.data/` 目录；与他人共享安装包或源码时，不要附带该目录。
